// Jest-DOM matchers for @testing-library/react (toBeInTheDocument, etc.)
// Uses the Vitest-compatible entry point — safe in both jsdom and node
// environments (no DOM APIs are called at import time).
/// <reference types="@testing-library/jest-dom/vitest" />
import '@testing-library/jest-dom/vitest';
